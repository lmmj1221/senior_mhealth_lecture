# Docker Compose 환경 변수 설정 예시
# 이 파일을 .env.docker로 복사하여 사용하세요: cp .env.docker.example .env.docker

# === PostgreSQL 설정 ===
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=senior_mhealth_dev
POSTGRES_PORT=5432

# === Redis 설정 ===
REDIS_PORT=6379

# === Backend API Service ===
API_PORT=8000

# === Backend AI Service ===
AI_PORT=8001

# === Frontend Web ===
WEB_PORT=3000

# === pgAdmin 설정 ===
PGADMIN_EMAIL=admin@senior-mhealth.local
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

# === GCP/Firebase 설정 (선택사항) ===
# 실제 GCP 프로젝트 사용 시 설정
# GCP_PROJECT_ID=your-project-id
# FIREBASE_API_KEY=your-firebase-api-key

# === 개발 환경 설정 ===
NODE_ENV=development
ENVIRONMENT=development
